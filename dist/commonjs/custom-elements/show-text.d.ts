export declare class ShowTextCustomElement {
    textValue: string;
    private created(pOwningView, pThisView);
    private bind(pBindingContext, pOverrideContext);
    private attached();
    private detached();
}
